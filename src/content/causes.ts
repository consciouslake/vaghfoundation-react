/**
 * Slugs for the four causes shared across the site, in the fixed
 * order they're authored everywhere they appear: home.pillars.items,
 * home.pills (the CauseBand), and the first four entries of
 * whatWeDo.initiatives. Anchors the homepage's cause links to the
 * matching section on the What We Do page.
 */
export const CAUSE_SLUGS = ['nourishment', 'wellbeing', 'learning', 'planet'] as const
